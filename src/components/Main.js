import g from 'glamorous';

import { rhythm } from '../utils/typography';

export default g.main({
  margin: '0 auto',
  maxWidth: '60em',
  padding: '0 32px',
  paddingTop: rhythm(3),
  flex: 1,
});
